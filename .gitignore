# Local-only helpers that are uploaded elsewhere (e.g. Namecheap cPanel)
_namecheap/

# OS
.DS_Store
Thumbs.db
desktop.ini
_project-context/

# Local tooling scratch (never deployed)
.audit_tmp/
.claude/
